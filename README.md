# Java Console CRUD Application

Консольное CRUD приложение, разработанное для управления сущностями Writer, Post и Label. Приложение реализует архитектурный паттерн **MVC** и взаимодействует с базой данных через **JDBC** (без использования ORM фреймворков).

## 🛠 Технологии

* **Язык:** Java 17+
* **Сборка:** Gradle
* **База данных:** PostgreSQL (или MySQL)
* **Доступ к данным:** Native JDBC
* **Миграции БД:** Liquibase
* **Тестирование:** JUnit 5, Mockito

## 📋 Функциональность

Приложение позволяет выполнять CRUD операции (Create, Read, Update, Delete) над следующими сущностями:

1.  **Writer (Автор)**
    * Имеет ID, Имя, Фамилию.
    * Связь: Один автор может иметь много постов (One-to-Many).
2.  **Post (Пост)**
    * Имеет контент, дату создания/обновления, статус (ACTIVE, DELETED) и автора.
    * Связь: Может иметь множество лейблов (Many-to-Many).
3.  **Label (Тег/Лейбл)**
    * Простая сущность с названием.

## 🗂 Структура проекта (MVC)

* **`model`**: POJO классы (Writer, Post, Label).
* **`repository`**: Слой доступа к данным. Реализован через JDBC. Использует `PreparedStatement` для защиты от SQL-инъекций.
* **`service`**: Бизнес-логика. Валидация данных, простановка дат и статусов.
* **`controller`**: Принимает запросы от View и передает их в Service.
* **`view`**: Консольный пользовательский интерфейс (Scanner).
* **`config`**: Конфигурация подключения к БД.

## 🚀 Запуск проекта

### Предварительные требования
* Установленная Java JDK 17+.
* Установленный PostgreSQL (или MySQL).

### Установка и настройка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/your-username/crud-console-app.git](https://github.com/your-username/crud-console-app.git)
    cd crud-console-app
    ```

2.  **Создайте базу данных:**
    Зайдите в ваш клиент БД (pgAdmin, DBeaver или консоль) и выполните:
    ```sql
    CREATE DATABASE crud_db;
    ```

3.  **Настройте подключение:**
    Откройте файл `src/main/java/org/post_hub/config/DatabaseConnection.java` и укажите ваши данные:
    ```java
    private static final String URL = "jdbc:postgresql://localhost:5432/crud_db";
    private static final String USER = "postgres";
    private static final String PASSWORD = "your_password";
    ```

4.  **Запуск:**
    При первом запуске **Liquibase** автоматически создаст необходимые таблицы в базе данных.

    **Через IntelliJ IDEA:**
    * Запустите класс `org.post_hub.Main`.

    **Через терминал (Gradle):**
    ```bash
    ./gradlew run
    ```

## 🧪 Тестирование

Сервисный слой (`Service Layer`) покрыт Unit-тестами с использованием JUnit 5 и Mockito. Тесты проверяют бизнес-логику изолированно от базы данных.

Запуск тестов:
```bash
./gradlew test
